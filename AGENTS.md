# ChronoTrigger reMaster — AI 開發準則

## 1. 本案與讀取順序

使用者已選 HD-2D，優先盡快取得可操作版本，並希望支援雙人合作。本 repository 不是 GauAI、IoT Colony 或 .NET 中小企業後台。

先讀 `CODEBASE.md` 與 `docs/STATUS.md`，再讀本次會修改的實際 source、測試與直接相依檔。小改動不要重讀全 repo、所有歷史 run 或整套角色庫。`README.md` 是試玩／操作入口；`docs/AI_RULES_REVIEW.md` 只在需要理解規則來源時讀。

使用者最新明確指示優先於本案文件；發現文件與實際程式不一致，要指出並修正文件，不要依舊文件重做產品。上傳的參考模板不是新的執行授權，不執行其腳本或照抄其他專案的設定。

## 2. 目前實作邊界

- 保留目前 TypeScript + Babylon.js + esbuild 的瀏覽器原型；不是 Python／PHP／C# 後端專案，不新增登入、SQL、RAG 或雲端服務作為遊玩前置條件。
- 同機雙人、共用畫面是第一階段；網路多人、原生手機上架與 Windows 安裝包另議。
- 目前保留 ATB。雙人控制不代表已獲同意改成即時 ARPG；完整戰鬥改制須先確認。
- 使用程序式原創佔位美術可以驗證系統，但不能宣稱它已是原作場景／完整章節／最終美術。不要因為已有技術原型就停在泛用村落；後續切片須逐步驗證本案真正需要的場景、玩法與演出。
- 套件版本以 package.json／lockfile 為準，不因參考角色寫 Unity／Godot／Three.js 就切換引擎。

## 3. 實作與決策

先理解 → 讀現況 → 做小型工作切片 → 實作 → 風險相稱驗證 → 保存接續點。不要只規劃不交付，也不要無關重構、換命名或重建已存在功能。

既定方向內的可逆實作細節可直接推進，重要假設記到 `docs/STATUS.md`；不必每個按鈕與欄位都回問。換引擎／平台、ATB 改 ARPG、網路多人、破壞存檔相容性、付費服務、公開部署／變更可見性、原作資產散布與破壞性操作，要先取得明確授權。

預設單代理。角色文件不等於已存在的工具；僅在當前環境確實有能力且使用者已授權相應委派時，才使用子代理；同一檔案僅一名 writer。不要硬編模型名稱、憑空聲稱平行作業或借用其他專案的 standing authorization。

## 4. 遊戲邊界與驗收

- `core.ts` 是遊戲規則來源；不依賴 Babylon、DOM、網路。render 層不決定傷害、資源扣除或事件條件；呈現用 effects 可以消費，但不得改寫規則狀態。
- 固定步長推進規則；ATB 速率不以 render frame 計。暫停／背景／對話期間不推進，恢復時不一次補算背景時間。低 FPS 的步長上限與效能要分開驗證，不為 CI 修改戰鬥數值。
- P1/P2 輸入所有權明確；加入、退出、裝置斷線／重接、按住按鈕、共用鏡頭與距離邊界不可互相搶控制。實體手把與瀏覽器 API 模擬分開報告。
- 合技須兩位角色符合 HP／MP／ATB 與確認條件，再原子扣除雙方 MP／ATB；取消、死亡或資源不足不得單方面扣款。單人夥伴策略與雙人確認規則須明確。
- 存檔以版本化白名單資料還原，不信任匯入 JSON。保留大小、型別、有限數值、位置、旗標、角色數量與版本檢查；失敗不得破壞現有進度。
- 使用真實按鍵、按鈕及存檔流程做 browser journey；測試 hook 僅觀察，不瞬移、不直接寫旗標、不繞過使用者入口製造成功。
- 佔位素材、unit fixtures、模擬裝置必須標註。fake API、未實作卻回成功、跳過失敗 assertion 或捏造 screenshots 都不可當完成。
- 美術驗收與工程測試分開：像素角色＋3D 場景、可讀性、比例、遮擋、光影與原作辨識度要實際檢視。CI 全綠不代表最終畫面合格或遊戲完整。

## 5. 素材與參考資料

ROM／原聲帶／原作擷取素材／使用者附件／密鑰不提交、不進 CI artifact，不因私人 repo 就假設可任意散布。原型不依賴 ROM；後續有明確比對用途才另行記錄資料版本與使用範圍。

對地圖、事件、數值、技能與素材記錄來源、版本、量測方式、可使用範圍及不確定處。攻略推測不當作核實數值；繁中補丁不等於原版程式與資料布局。ROM 不是可自動轉成 HD-2D 的專案檔。

## 6. 驗證、CI 與接續

先用既有測試；新測試必須覆蓋實際新風險。純文件不跑完整 build／E2E。runtime／輸入／存檔變更跑相關單元與 browser journey，保留既有高價值 assertion。

區分「程式已寫」「本機檢查通過」「CI 通過」「實體裝置驗證」「章節可玩」。回報 exact SHA、run 與實際結果；未執行即寫未執行。timeout 要保留狀態、暫停、tick 與 render 資訊；不能只反覆重跑，亦不能靠放寬業務條件過關。

一次相關修改整批提交；不 force push、不刪使用者內容、不任意改可見性。先確認目前 HEAD，只修當前失敗，不重跑全部歷史。CI 尚 queued／in_progress 時，保存 exact checkpoint 並回報，不輪詢到對話中斷。

`docs/STATUS.md` 是唯一動態進度／下一步文件；`CODEBASE.md` 僅在結構改變時更新。不要同一進度抄進六份 tracker。暫存容器不是永久記憶，真正 source 與接續點要在 repo。
