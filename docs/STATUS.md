# Current status — 2026-09-18 / CI18 registered

**FULL GAME IN PROGRESS — 0.8.2 IMPLEMENTED; CI17 GOOD-ROUTE ORGAN APPROACH REPAIRED; CI18 ACTIVE; NOT WHOLE-GAME OR 90-POINT ACCEPTED.**

Repository `KartChang/ChronoTrigger_reMaster`; main only, non-force. Sole validation source **`8c6f8fb97ef96718d542d1c1f29d44bade46d969`**, tree **`ca6cdc1bebf172847dbb9695db7e56e5ab602d49`**, parent `ed9f229452b65a04c4d5eaf1bbd959d0f910d98f`. Commit creation, non-force main update and actual ref readback completed. All nine changed code/test/workflow files were read back individually; their blob SHAs match the local tested files in `evidence/CI18_LOCAL_VALIDATION.json`. A later docs-only [skip ci] HEAD is not another validation candidate.

## Sole current validation

**CI18 `35344366293`, push, attempt 1**, created `2026-09-18T12:23:05Z`. One exact-source run query returned queued; the following single jobs read returned:

| Job | ID | Saved observation |
|---|---|---|
| validate |105597385016|in_progress; setup-node running|
| Fresh story and hearing (good) |105597385166|in_progress; build running|
| Fresh story and hearing (bad) |105597385342|queued|

These are registration observations, not terminal results. **Source frozen; no manual dispatch, cancellation, rerun or repeated polling.** Receive this same run after the completion notice. All three jobs must succeed. New host-unit step is validate step10; existing browser steps now11–20. Diagnose by actual step name/report, not stale numerical indices. Machine checkpoint: `evidence/CI18_CHECKPOINT.json`.

If pending, preserve/report without waiting repeatedly. If failed, inspect its first actual failing step and same-run report/log/failure PNG; use organApproaches/chestApproaches/probeBefore/probeAfter/inputContext and source/HTML hashes. Do not assume the next failure has the same root. Keep real keys, real exported saves, collision and existing event/resource assertions. If all three succeed, retain five matching artifacts in the project Drive folder, inspect actual repaired interactions and keyboard/presentation frames, then verify the matching Pages deployment.json/source/artifact/HTML hashes. Do not deploy a failed run's playable ZIP just because it exists.

## CI17 received; actual failure and completed batch

CI17 `35333606073`, source `3833d42b63bcce65ab055b1d08d0fb88ece200fe`, completed **failure** at `2026-09-18T10:46:50Z`. Validate105563278252 succeeded in every browser step, including the old chest root, rescue and trial/prison/tank/inventory/future arrival. Bad105563278477 succeeded. Good105563278493 failed step9 before the organ, not the chest or verdict.

Good's actual report, job log and original-resolution PNG agree: P1 x=-5.116666666666669/z=6.166666666666663, cathedral/allied, organ closed, focused canvas `world`, unpaused, visible `E · 管風琴`. The old driver insisted on x<=-6.8 inside the inflated solid organ after northward release drift; the south edge is z=6.15. The character was already within the existing1.8 interaction radius. Full terminal/artifact index: `evidence/CI17_TERMINAL.json`; actual-screen review: `evidence/CI17_REVIEW.md`.

This coherent nine-file batch adds a shared real-input approach to organ and chest: use the aisle, stop only on correct visible prompt AND physical distance, send normal E through the caller, and retain post-release readiness. A read-only predicate rejects modal/pause/focus/ownership/map/mode/clock changes and wrong or hidden hints. Final-leg cleanup attempts every held key release even if a key call fails, and records failure context rather than retrying or fabricating success. Both rescue and fresh good/bad routes consume it. Real browser scenarios now also export/reload their own organ progress (v5 rescue, v6 fresh conduct), re-interact and check stock/flags without synthetic saves.

**Local full check: 568 Node tests passed,0 failed,0 skipped; typecheck/build/assets passed. Ten Python host-unit tests and compileall passed.** Added53 Node cases cover the observed root, solo/paired approaches from Frog/locked door, polling1/6 ticks, release0/5/12 ticks, one-time supplies/v5, and predicate rejection. Python doubles validate host cleanup and diagnostics; they are not browser acceptance. One host-test step was added to the existing validate job; all three jobs, existing browser scenarios and timeouts remain. **No src runtime, balance, collision, save schema, art, build script or Pages change.**

Local Chromium was actually attempted with an unmodified CI17 kingdom export, but returned `net::ERR_BLOCKED_BY_ADMINISTRATOR` before game load. No policy bypass, timeout-triggered kill or local browser pass claimed. CI18 must supply the new genuine browser result.

## Evidence and durable recovery

Project Drive folder **`1UhnvGAlVgAySLaV2Oka0LjNidTaxMeEb`**. Latest **`Chrono-CI17-terminal-CI18-interaction-batch.zip`**, ID **`18S4M1D7gkB8L0S-fIVnaKM2V0_SFt1uD`**, **18567674 bytes**, SHA256 **`d8e997083493d14df65b48ee26056df87c3893f86b5f639df85e267e87cbf45b`**. Correct parent, whole raw readback/hash/ZIP CRC and all23 manifest records verified. Contains all five untouched CI17 artifacts, nine tested modifications, terminal/review/local receipts, real local logs and the clearly labeled derivative contact sheet. It was sealed before publication; later commit/run metadata is here on GitHub, not retroactively inserted in the package.

CI17's playable0.8.2 exists because validate passed, but **the complete run failed and is not deployment-eligible**. No new Pages acceptance/restart/dispatch. Last retained accepted preview remains0.8.1 CI15/Pages8, not a fresh live-site assertion. Original-resolution review covered organ failure/chest/tank; six other same-run images were inspected as a resized contact sheet. Repeated floors, sparse rooms/block props and occasional companion overlap remain; no final-art/hardware score.

If the container is erased, use current main and only necessary retained packages. Exact CI17 source is also inside its browser artifact. Pinned dependency toolchain is already in prior Drive bundle `1JItxu6LhYFyTwMysm7mlY4lQsClUrvjE`; restore only node_modules, never old source/config. No duplicate bootstrap CI or historical branch audit. Old0.8.2 recovery ZIPs are superseded. Private ROM remains private/unextracted and outside source/CI/Pages. Delivery history: DELIVERY_INDEX.md.

## Remaining scope

Current regression closure is not completion of T03/T04. After CI18 acceptance and matched deployment, continue original hidden rules/version differences/full map topology and equipment/status/growth/currency/shops/roster/skills. T05 full art/animation/rights-cleared sound, T06 remaining eras/main quest/sidequests/endings and T07 complete-game>=90 plus physical-device/performance gates remain open. Preserve P1 Crono/P2 Lucca and autonomous story companion; no P3 or ARPG rewrite.2300 arrival is not the future campaign. No reduced denominator, fabricated score, or repeated accepted chapter implementation.
