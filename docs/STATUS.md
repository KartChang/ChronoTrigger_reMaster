# Current status — 2026-09-18 / CI17 registered

**FULL GAME IN PROGRESS — 0.8.2 IMPLEMENTED; CI16 ROOT REPAIRED IN TESTS; CI17 ACTIVE; COMPLETE BROWSER/PAGES ACCEPTANCE PENDING; NOT 90-POINT ACCEPTED.**

Repository `KartChang/ChronoTrigger_reMaster`, main only/non-force. Current exact validation source **`3833d42b63bcce65ab055b1d08d0fb88ece200fe`**, tree **`a99bd9be894b396e5c0d1c512a1e7d8fb98b898d`**, parent `8ae191f46aa4f2fd1b4408f90406dea566d8e5a5`. Commit -> non-force main update -> actual ref readback completed. All five changed test-file blob SHAs were read back and matched local tested bytes. Subsequent [skip ci] documentation does not create another validation candidate.

## Sole current validation: CI17

**Run `35333606073`, number17, push, attempt1**, created `2026-09-18T10:13:33Z`. First registration read: run and all three jobs **in_progress**, conclusion null. This is a saved observation, not a later terminal result.

| Job | ID | Saved registration observation |
|---|---|---|
| validate |105563278252|npm ci success; npm run check in_progress|
| Fresh story and hearing (bad) |105563278477|npm ci in_progress|
| Fresh story and hearing (good) |105563278493|npm ci success; build in_progress|

Source frozen. No dispatch, cancellation, rerun, workflow edits or long polling. This source push started one existing full CI. Await completion notice, then receive this same run once. All three jobs are required. Receipt: `evidence/CI17_CHECKPOINT.json`.

If pending, preserve/report without repeated polling. If failed, inspect the first actual failed step and its same-run logs/report/lastObserved/failure.png, including inputContext/focused and chestApproaches where applicable. Do not infer that a later failure is the same chest root. Do not weaken assertions, manufacture saves, inject state, remove collisions or merely extend timeouts. If all three succeed, preserve the five same-run artifacts, inspect actual screenshots/reports and only then verify the Pages deployment whose deployment.json/source/playable artifact/HTML hash match this exact CI.

## CI16 received and its first root repaired

CI16 `35328892846`, source `490218402b493cc69296c5de4afa278be7471223`, ended **failure**: validate105548368148 failed step18 rescue/passage chest; bad105548368471 and good105548368477 succeeded. Validate keyboard and steps10–17 passed; separate trial/prison/tank step19 was skipped. CI16 is not whole-batch accepted. Full records: `evidence/CI16_TERMINAL.json` and `evidence/CI16_VISUAL_REVIEW.md`.

The original route first walked to z=-6.15, then x=-7. Real key-release drift left P1 at x=-5.6/z=-5.8 against the inflated solid chest, with visible E・木箱. The chest's south collision edge is z=-5.95; increasing the westward hold cannot cross it. This is a test walking-route defect, not evidence that the runtime should allow walking through the chest.

The coherent repair covers rescue plus both witness routes via shared `tests/rescue-route.json`/`rescue_route.py`: retain the entrance aisle z=-6.8 before turning west; check actual distance and visible chest prompt; retain all original one-time stock, save, combat and story assertions. Diagnostics now retain after-release coordinates, focus/UI context and the attempted route even on failure. Eleven added regressions reproduce the original collision, test solo/paired release latency and v5 stock persistence, and verify reachable collision-checked interaction routes across cathedral/passage/sanctum. This topology test coverage is not original full-map fidelity certification.

**Fresh local validation: 515 passed,0 failed,0 skipped; typecheck/build/assets/Python compile passed.** `src`, runtime rules, save versions, build scripts, workflow and site were not changed in this repair; gameplay implementation remains the published0.8.2 work from490218. Local Chromium was attempted with a genuine unmodified CI16 save but blocked before game load by `net::ERR_BLOCKED_BY_ADMINISTRATOR`; no policy bypass or local browser pass claimed. Pinned toolchain and logs are retained. Details: `evidence/CI17_LOCAL_VALIDATION.json`.

## Real screen review and acceptance limits

The same-run keyboard report passed all six checks, including victory Enter continuation, real export/import focus recovery and held-input ownership isolation; all five recorded focuses were `world`. Actual home/fair/idle/battle-ready/import/court/cell/failure PNGs were inspected. Victory continuation is report evidence, not a separately supplied victory screenshot. Atlas previews and material flags were not substituted for gameplay. Repeated floor patterns, sparse props, mixed NPC detail and compressed layouts remain open; no final-art, hardware or whole-game90 approval.

CI16 produced four actual artifacts, all retained; **no playable artifact was produced** because validate failed. No local HTML was substituted. 0.8.2 deployment remains unverified. Pages9 was an earlier automatic site push, not a second game CI or proof of the selected gameplay. No Pages restart/dispatch was made. Last accepted preview remains the retained **0.8.1 CI15/Pages8** acceptance, not a fresh live-site assertion.

## Permanent preservation

Project Drive folder **`1UhnvGAlVgAySLaV2Oka0LjNidTaxMeEb`** only. New **`Chrono-CI16-terminal-CI17-route-batch.zip`**, file **`1JItxu6LhYFyTwMysm7mlY4lQsClUrvjE`**,35691788 bytes,SHA256 **`72f6ae721f07e5775939b89c4a6e4b74d8574116d89d59a483f3c33a48b27d15`**. Correct parent, raw download/full ZIP hash/CRC and all20 manifest records verified. Contains four untouched CI16 artifacts, five repaired test files, full terminal and visual receipts,515-test and blocked-browser logs, contact sheet, local build and pinned dependency toolchain. The local build is not an accepted CI playable. Package was sealed before publication; source/run publication authority is this GitHub checkpoint, not a claim that the package already includes its own later commit ID.

Earlier runtime package `1RhFYQsR3vkqEDvoD0X_75PbZRkD6s8uM` remains valid for490218's runtime; newer test repair/current documents come from main. CI15/Pages8 bundle `1AKR6UJGdOB0fxzW6trUTlCdpbmuPwel6` is already retained; no duplicate historical download is required. Last accepted source `2a689fe5abc76d865bc15bc948a43f9738bd6650`, CI15 `35315967536`, Pages8 `35317920193`, HTML SHA256 `8a3a09e2557540e389446a31a592a462d62bd1c4f22567f8988efa44604f9a9b`.

ROM `1yMJ5jL8UeUi60D60BZ9Ffy1cyme3EpvM` remains private and not extracted/emulated/publicly bundled. Old9e4feb staging and38-file recovery ZIP are superseded; never reapply them. If the container is cleared, restore current main, not old source. Toolchain is now durably inside the new Drive bundle; restore only dependency files from it, not old packaged source/config.

## Remaining existing TODO

After current exact CI/real-screen/Pages closure, resume T03 original hidden rules/version differences/full topology, then T04 equipment/status/growth/currency/shops/roster/skills. Continue T05 complete art/animation/music, T06 remaining eras/main quest/sidequests/endings, T07 whole-game>=90 and physical devices, T08 cloud receipts each batch. No existing chapter/framework rebuild, no P3/ARPG conversion; P1 Crono/P2 Lucca and autonomous story companion remain.2300 arrival is not the whole future chapter.515 tests are not a score; the old30 is stale; no reduced denominator or new numerical score.
