# Current status — VQ01J / CI27; merchant repaint validation

Updated 2026-09-19 UTC (2026-09-20 Asia/Taipei). Main only, non-force. STATUS and handoff/IMMEDIATE_CONTINUATION.md are execution authority. Early T05 visual/play comfort remains first. Full scenes, physical devices and final90 are not accepted.

## Current source and only new validation

VQ01J / 0.9.9 source **4f8273035bb39f82b38d09d1973e4a4f09f1c63c**, tree **5f47255d4fc8c77f070838a1afad1d2b3e6d8982**, parent **3251f0a12e8233a3a70bd4f0f784560b0216dbd5**. Eight code/test paths published in one non-force commit; main readback and compare confirmed eight paths, one ahead, zero behind. Complete remote src/scripts/tests subtrees and index blob match the locally tested files. Current main docs, assets, workflows and all unrelated code were preserved; old source-archive documentation was not promoted.

Only validation **CI27 35460638960**, push / attempt1, workflow360357259 / .github/workflows/ci.yml. Created and updated at the last observation **2026-09-19T18:15:57Z**, status **queued**, conclusion null. Exact-source all-event/all-state run count one. No manual dispatch, retry, cancellation or long polling. Freeze runtime for this run; later [skip ci] documents do not change it or require another CI. Machine-readable checkpoint: evidence/CI27_CHECKPOINT.json.

## Implemented batch; browser acceptance pending

The existing equipment panel builds its replacement off-DOM and publishes the complete content once before restoring the original action/row focus and content scroll. At the actual merchant, compatible catalog comparisons remain visible after the last owned copy is sold, explicitly labelled unowned and disabled. Outside merchant range, the existing owned-only equipment list remains. No inventory copies, gold, prices, bonuses, story facts or save schemas are invented or changed by reading this catalog.

Equipment comparison/status lines and available/unavailable trade reasons now have stable structure. Desktop buy/sell columns keep the same width; the original small-screen two-column layout, scrolling, readability, coarse-pointer targets and forced-colors fallback remain. Explicit scroll restoration, rather than browser scroll anchoring, owns a repaint. Disabled and detached stale action callbacks are rejected before executing a trade/equip or emitting misleading feedback.

The real keyboard last-copy sale now records before/after DOM metrics, screenshots and two painted-frame observations without state setters or artificial time. The original <=2px scroll assertion is retained, with additional row-position/visibility and catalog-shape checks. The raw measurement report is saved before assertions; failed equipment reports now include exception type and traceback. All **80 original equipment assertions remain**, now82 in that driver; original same-run native imports, v8/IndexedDB, wallet/stock, ATB, victory and seven fine/two coarse viewport cases remain intact. No timeout-only relaxation.

Fresh **755 Node / 48 Python unit tests**, assets/typecheck/build/Python compile passed. Nine new Node and eight Python cases were added. Against the prior production implementation, eight new tests fail and all746 original Node tests pass; after the change all755 pass. The range-clamping port and synthetic geometry are unit evidence, not browser layout or physical-device evidence. No local browser operation was retried. Local HTML5593404bytes/SHA256 **46c4757318823e2c350dd5b96f5fc88563931827fba0a7e619ca38003a7faef6** is explicitly unaccepted. See evidence/VQ01J_LOCAL_VALIDATION.json.

## CI26 terminal — first-meeting route passed; equipment scroll failed

CI26 **35458116851**, source **cca5690500d4ff7954698de406da736c957d2bc3**, completed failure. Validate **105936920731** failed step13; steps14–21 skipped. Keyboard step11 and complete prologue step12 passed, including both genuine contact orders and six early viewport cases. Witness good **105936921017** and bad **105936920529** both succeeded. Keyboard/prologue native-import records each contain five attempts, equipment completed its first native import, and each witness completed ten. This does not make the whole batch accepted.

The exact first failure is after selling the last **bronze-helm**. Gold90, owned helmet0, disabled sell action and focus on **equipment-shop-row-bronze-helm** all passed, then **abs(content.scrollTop-prior_scroll)<=2** failed. The old report did not record the before/after geometry or the exact pixel difference; failure text was empty. Production removed the last owned equipment card, changed reason-line structure and cleared/rebuilt a live subtree. A local constraint-port test reproduces the resulting scroll-range risk, not the exact historical browser layout or a uniquely proven CSS cause. The fix is implemented but remains unaccepted until CI27.

Actual equipment/failure.png and the portrait initial-meeting image were inspected. Merchant stock/gold and safe focus row were visible; early interaction cues were readable and separated. Repeated paving and simplified scenery remain. Four original CI26 artifacts matched provider sizes/digests and ZIP CRC; exact source-tar tree matched. See evidence/CI26_TERMINAL.json. No playable artifact or new Pages accepted. Inventory viewport and equipped-combat checks were not reached. CI25's first-meeting issue is no longer the current root; do not rerun that unchanged candidate or reopen closed chest/organ work.

## Durable delivery and recovery

**Chrono-CI26-terminal-VQ01J-merchant-stability.zip**, Drive **19GBm7cW9fD5CgQhu3sDdZDHDr907jouR**, only project folder **1UhnvGAlVgAySLaV2Oka0LjNidTaxMeEb**. **9809102 bytes**, SHA256 **3b548d1b286e54c00400cf6abeff59d41cb854ba1fdb9b046b971be97964b0d6**. Final raw download, correct parent, whole hash, ZIP CRC, **21 manifest entries / 8 tested deltas** verified. It retains four unchanged CI26 artifact ZIPs, source/test deltas, red/green logs, receipts and labelled unaccepted preview. The exact cca source tar is inside the browser ZIP. No ROM, original media/fonts, dependencies, credentials or new local screenshots. Archive receipts precede publication; CI27_CHECKPOINT records remote publication/run/cloud verification. Current GitHub documents override older archive documents.

Last fully accepted remains **0.9.4**, source **69dc0524b593df6948c11397280bfcedc841236f**, CI21 **35440863455**, Pages15 **35442701457**, retained bundle **19GRN41beDiTxDvZk_6LlYTYApXfmrfQ5**. All3jobs/23reports/seven fine-pointer menus were accepted, not physical touch or full-scene quality. CI20/Pages14 and chest/organ roots remain closed; no unchanged replay.

Held VQ01+B+C rooms/materials/furniture/cloth/stalls/bell/telepod/actors/camera/occlusion/foot-contact/easing already exist. Recover A→B→C working-delta only when needed, with DELIVERY_INDEX hashes; current UI/docs win and overlaps require merging. Do not repaint, reapply old independent-ui or promote incomplete staging. The restricted renderer write and local browser operation were not retried/bypassed. **src/prologue-render.ts remains2711a74185aacf3c6bddf9db85ba99a2afbc507a**. Toolchain restoration is node_modules only with esbuild hardlinks. ROM stays private.

After E2E notice read exact CI27 once. Pending: save/report without polling. Failure: first same-run root with merchant-repaint-report, equipment traceback/observations/PNGs or the actual failing driver. Success: all3jobs, retained initial-meeting/keyboard/native-import/equipment/economy/ATB/victory/viewport checks and every original journey; retain artifacts, review pictures and verify matching Pages/source/HTML. Then continue permissible early scene/actor/animation/camera/HUD comfort before later expansion. Full T03–T08 scope, home-to2300 route and v1–v8 remain. Arrival is not the full future; old30 remains stale, not a new score. No P3/ARPG/framework restart.
