# Execution TODO — 2026-09-18 handoff

本表把既有 STATUS 的下一步整理為可接續任務，不是重新規劃或重做專案。Exact source/run/status 只以 `docs/STATUS.md` 為準。全部分支僅此 AI 使用；保持 main 單線、非 force 提交。已實作但待驗收，與尚未開發必須分開。

## 目前唯一優先任務

### T00 — CI12 修道院完整验收【當前；等待既有 run，不是未開發】

讀 CI12 `35243837438` 的最終結論；現行前六段成功，第七段 rescue_browser.py 執行中。失敗只修真實第一根因；成功保存新報告、截圖、試玩與素材 artifact 至 Drive 並列 source/run/ID/hash。禁止重做修道院／第三隊員／v5，也禁止另開同一 source 的重複 CI。不要以未完成的美術門檻阻止收集功能驗收證據。

### T01 — 0.6 畫面檢查與 Pages 更新【依賴 T00】

檢查實際 cathedral、three-actor battle、Yakra、reunion 畫面及報告，確認鏡頭、遮擋、提示、動作與存讀檔。沿用既有 Pages workflow；只接受通過 CI 的 exact artifact，核對 deployment.json source/hash，不能把舊0.5.1稱為0.6。保存 browser evidence 與發佈收據，不重新做部署系統。發現缺陷局部修正；未發現則繼續內容，不無限重審。

## 後續實作順序（延續既有方向；不要平行多候選）

### T02 — 補上真正的原作初始開場【未實作】

在已存在 fair 路線前增加家中醒來、前往千年祭、初遇與項鍊相關事件；先核對明確版本的原版圖／行為，再寫資料。保留現有 fair/canyon/kingdom/rescue 進度入口與舊存檔；不從零重造事件引擎。將將來審判會用到的選擇記為白名單旗標，原作不確定部分標明，不猜成精確規則。

### T03 — 審判與越獄【未實作，接 returned checkpoint】

從已存在的 rescue.stage=returned 接護送／王城審判／囚禁／逃出路線；利用 T02 的明確旗標，不硬判所有舊存檔同一行為。沿用 ATB、地圖、輸入、存檔、動畫與測試模式；只擴充必要 schema，舊 v1–v5 仍可匯入。新切片須真正走路／對話／戰鬥／存讀檔，不能只加文字標記。

### T04 — 背包、裝備、成長與角色／技能管理【部分基礎已有，整套未完成】

保留已完成的一次性回復藥及扣除原子性，增量建立物品資料、背包／裝備 UI、貨幣／商店、經驗與能力成長；分離 UI 與規則。完整 roster／控制權切換、技能學習與二／三人技仍待補；不得把第三自主同伴誤稱整套 roster 已完成。

### T05 — 原版對照美術、動作、音樂持續提升【持續，與新切片合併交付】

已有 reference index、繪製函式、PNG/JSON 匯出、角色姿勢與素材登錄，不另建重複管線。依實際原版圖優化，不以生成海報當原版或現有截圖。補齊需用素材與音訊再在引擎驗證；一次完成一組場景與戰鬥資產，不先等待全遊戲素材完工。完整迷宮拓樸、更多動作與配樂仍是缺口；新增 source 必須更新素材來源，原作檔／ROM 不公開上傳。

### T06 — 後續時代、主線、支線與結局【未完成】

在前述實際可玩接點逐章增加其餘世界與內容，先取得相關版本參考；不要以十個空場景取代主線。原有章節只因真實 bug 或新增內容需求做局部相容修改，不能重做前六個里程碑。

### T07 — 整體 90 分與實體裝置驗收【最終門檻，不是先自評通過】

每切片保留缺口，整體總分>=90、各面向>=80%、無 critical blocker、五項證據 gates 與必要資產通過。真機 keyboard/gamepad、FPS/frame time、載入／記憶體、背景與存檔要有實测；軟體GPU不等於玩家硬體。瀏覽器仍優先，原生App、網路多人、即時ARPG未列已同意的重寫範圍。

## 永久交付規則

T08（每批都執行）：source 與進度提交 GitHub；可玩包／原始碼快照／素材／報告／截圖保存 GitHub artifact 後再留存 Drive，記錄版本、exact SHA、CI、雜湊與 URL。不要只貼聊天附件。私有依賴工具鏈只有必要時恢復，不要求使用者提供權杖；工具與權限先實查。臨時路徑可消失，不能列為唯一 authority。

不允許把文檔持久化本身當成新的遊戲功能完成；也不許刪掉未完成項目湊90分。
