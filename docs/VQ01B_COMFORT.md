# VQ01B：前段遮擋、接地與窄畫面（工作版本0.9.2）

本批接續已保存的VQ01，不重畫已完成資產、不擴充後續劇情，也不重試已被攔截的程式寫入。新增／修改13份程式、測試、設定；累計未發布差異為35個檔案。GitHub main仍是0.9.0遊戲，最後已驗收Pages仍是0.8.2。以下是本機實作，不是已上線功能。

## 鏡頭、遮擋及接地

臥房、家中與千年祭在窄直向畫面改以角色為中心平移，不再一律把完整房間壓成縮圖；多人分開時會依真正可見角色和敵人範圍拉遠，保留上方工具列及下方戰鬥資訊的安全範圍。大地圖仍維持縮尺全景，其他章節沒有被套用這個鏡頭政策。沒有更改任何遊戲座標或碰撞。

桌子、窗簾與三個攤位棚布改以明確物件群組處理遮擋。射線與正交鏡頭同向平行，並檢查實際三角面，不只憑大包圍盒猜測；當擋住角色時只淡化該物件群組，不會把相同材質的牆、另一攤位或角色一起淡化。保留完整碰撞。淡入淡出使用模擬tick，暫停時不繼續，離開遮擋有短暫緩衝避免閃爍，換地圖／重載時還原原始可見度。

修正桌底陰影原本低於地毯而被蓋住的層次；母親與前段祭典NPC使用相同的腳底錨點計算。實作檢查也發現固定alpha-test窗簾不能僅靠mesh visibility取得混合效果，已將可淡化窗簾限定到正確的材質模式，沒有改其他角色的透明規則。合併幾何時保留遮擋群組，避免為了減少draw call而失去逐攤位淡化邊界。

既有公開版與測試版使用同一套呈現。讀取介面新增實際sprite頂點投影範圍；原有新遊戲腳本會在桌面、650與390寬度檢查這些投影，不只重讀鏡頭演算法自己估計的範圍。

## 已完成的本機檢查

新鮮完整check：**706項Node通過（新增43），16項Python通過**，typecheck／build／素材／Python編譯通過。實際Babylon矩陣、原場景建構器、透明分類、群組合併、延遲場景建立、暫停／重載／移除與既有遊戲測試均涵蓋。第一次開發測試699／701的原始日誌保留；私有Vector3標記比較與真實的第一幀幾何發現問題均已修正，沒有靠增加等待時間通過。

CPU幾何探測66個明示的單元測試座標：27個被實際模型遮擋、39個通視。這些座標經既有通行判定檢查，但不是玩家走過的劇情、存檔或瀏覽器紀錄。99次射線測試是該組單角色樣本的最大值，不是全遊戲負載上限，也不是FPS測量。

本批沒有執行新的瀏覽器，沒有新版遊戲截圖。原本發布攔截沒有新的允許結果，沒有重試、換編碼或換管道。未修改被攔截的src/prologue-render.ts；本次雲端增量包不重送該檔或包含它的完整執行檔。既有原生選檔器與同run存檔斷言仍保留。

## 已保存與尚待驗收

Drive **1vkPuPN8HN46d1uHaJY9__OPrf455k3QV**，107554bytes，SHA256 **084c472e69f4f44ed72fd8f9a9276f2756aa739ef592227a0d8f1f4d069871d8**；下載回讀／CRC／22項manifest均一致。內容是13份增量檔、實際日誌、CPU幾何報告與完整檔案雜湊，不是再一份只有規劃的清單。與原VQ01完整包搭配還原，詳見DELIVERY_INDEX.md及ZIP內RESTORE.md。

**前段90分、實際構圖／透明混合／動作／流暢度與裝置驗收仍未通過。** 光有射線和矩陣測試，不能證明玩家已經覺得舒服。正常發布操作獲允許後，須先完成整批提交與一輪完整CI，再取得實際畫面和Pages匹配證據；後續章節仍暫緩。原作地圖、托魯斯／王城／法庭細節、完整NPC與角色動作、音樂及真機效能均保留，完整遊戲範圍不縮減。
